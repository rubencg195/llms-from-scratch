[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra -q
markers =
    slow: heavy training / long-running lab (skip with -m "not slow")
    network: lab downloads a dataset (skip with -m "not network")
    extra_deps: needs packages beyond requirements.txt (e.g. scikit-learn)
